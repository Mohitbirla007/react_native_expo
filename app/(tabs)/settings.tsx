import { ThemedText } from "@/components/ThemedText"
import { SafeAreaView } from "react-native"
export default function SettingsTab() {

    return(
        <SafeAreaView>
            <ThemedText> Settings tab </ThemedText>
        </SafeAreaView>
    )
}